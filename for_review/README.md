# student_dashboard — Review Snapshot

This folder is a compact snapshot for quick review. It contains only the core app source and assets references so your professor can find the main code quickly.

Included:
- `lib/` — application source files
- `pubspec.yaml` — minimal asset declarations
- `README.md` — this file

Notes:
- This is a read-only snapshot for review and does not include platform build folders (android/ios/web/etc.). Use the root project for running the app.
